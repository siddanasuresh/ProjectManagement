import { TaskmgrSearchPipe } from './taskmgr-search.pipe';

describe('TaskmgrSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskmgrSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
