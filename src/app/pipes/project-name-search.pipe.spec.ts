import { ProjectNameSearchPipe } from './project-name-search.pipe';

describe('ProjectNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
