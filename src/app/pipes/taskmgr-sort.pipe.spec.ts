import { TaskmgrSortPipe } from './taskmgr-sort.pipe';

describe('TaskmgrSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskmgrSortPipe();
    expect(pipe).toBeTruthy();
  });
});
