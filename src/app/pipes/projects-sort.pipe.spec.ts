import { ProjectsSortPipe } from './projects-sort.pipe';

describe('ProjectsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
