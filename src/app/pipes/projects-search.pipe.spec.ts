import { ProjectsSearchPipe } from './projects-search.pipe';

describe('ProjectsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
