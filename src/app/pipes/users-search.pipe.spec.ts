import { UsersSearchPipe } from './users-search.pipe';

describe('UsersSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UsersSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
